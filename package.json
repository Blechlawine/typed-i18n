{
    "name": "typed-i18n",
    "version": "0.0.0",
    "private": true,
    "workspaces": ["apps/*", "packages/*"],
    "scripts": {
        "dev": "bun --filter '*' dev",
        "lint": "bun --filter '*' lint",
        "format": "biome check --apply-unsafe . && biome format --write ."
    },
    "devDependencies": {
        "@biomejs/biome": "^1.7.3",
        "@types/bun": "^1.1.1"
    },
    "module": "index.ts",
    "type": "module",
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
